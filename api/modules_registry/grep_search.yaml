id: grep_search
name: Pattern Search (grep)
description: "Search files for regex patterns — IOCs, keywords, base64 strings, encoded commands."
input_extensions:
  - "*"
available: true
run_on_files: true
category: Search
tags: [grep, regex, ioc, keyword, pattern, search]
config_keys:
  - patterns
